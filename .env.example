# Port de l'application
PORT=3000

# Chemin du fichier de base de données (utilisé uniquement par `npm start` /
# `start:prod`, voir STORE_DRIVER dans package.json) — optionnel
# DB_FILE_PATH=data/db.json
